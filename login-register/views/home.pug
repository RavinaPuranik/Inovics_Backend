extends layout
block content
    p Welcome
    br
    if user
       h1 Hello #{user.name}
       br
       img(src=`/uploads/${user.photo || 'user.jpeg'}` height="200" width="200" align="right")
       a(href="/logout") Logout
    else
       a(href="/login") Login
       br
       a(href="/register") Register
